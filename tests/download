.env
node_modules/
dist/
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Storage state generated by the auth setup project — must be regenerated
# on every run, never committed (stale sessions / secrets risk).
.auth/*.json
